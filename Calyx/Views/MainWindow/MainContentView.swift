import SwiftUI

/// Placeholder SwiftUI view for future use.
///
/// Phase 1 renders the terminal directly via AppKit's SurfaceView
/// added as a subview of the window's contentView.
/// This SwiftUI view will be used in Phase 3+ when the sidebar
/// layout and other chrome are introduced.
struct MainContentView: View {
    var body: some View {
        Color.clear
    }
}
